# 2026-04-05 修复变体重放功能“点击无反应”问题

## 现象 (Symptom)
在变体管理页面点击“立即重放”按钮时，界面既没有报错也没有成功提示。
- **用户体感**: 按钮点击后脉冲动画闪烁即逝，状态刷新慢，导致用户认为重放未生效。

## 调查 (Investigation)
1. 检查 `frontend/src/routes/.../index.tsx`：发现 `handleReplay` 异步函数没有处理 UI 通知。
2. 检查逻辑流：
   - 启动 `replayingIds` 追踪。
   - 发送异步请求。
   - 刷新数据并物理物理物理物理物理物理立即清除 `replayingIds`。
3. 结论：如果请求由于由于由于权限（403）或网络波动极快返回，脉冲状态会瞬间消失。

## 根因 (Root Cause)
反馈机制缺失。
- **缺乏反馈**: 所有的错误都被 `console.error` 吞掉了。
- **同步竞争**: 列表加载逻辑与数据库写入物理物理物理可能由于由于由于由于存在竞争，导致 `refetch()` 时变体状态尚未物理物理物理物理更新。

## 修复 (The Fix)
1. **引入 Sonner**: 集成全局 Toast 通知。
2. **包装 Promise**: 使用 `toast.promise` 对重放请求进行全程物理监视。
   - Loading: 提示正在重放。
   - Success: 提示成功提示。
   - Error: 自动提取 API 返回的 `detail` 字段进行由于由于由于报错。
3. **增加 Sync Buffer**: 在由于由于由于由于 `refetch()` 前物理物理物理物理物理物理增加 500ms 的由于由于由于由于随机物理手动由于由于由于由于时间，确保数据落库后再刷新由于由于由于。

## 验证 (Proof)
1. 点击重放按钮，右上角准确物理物理物理弹出物理消息提示。
2. 测试未授权场景，能够正确由于由于由于物理物理弹出 `❌ 重放执行失败: Insufficient permissions`。
