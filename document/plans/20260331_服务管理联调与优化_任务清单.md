# 服务管理模块联调与优化 - 任务清单 [2026-03-31]

- [x] **后端优化 (Backend)**
    - [x] `system_modules.py`: 将 "unknown api" 加入 `EXCLUDED_SERVICE_NAMES` (大小写修正)
    - [x] `worker.py`: 检查并加固提取服务名逻辑，预防 "Unknown" 命名的模块生成
- [x] **前端 Hook 封装 (Hooks)**
    - [x] `useSystemModules.ts`: 实现 `useUpdateModule` (状态/负责人更新)
    - [x] `useSystemModules.ts`: 实现 `useDeleteModule` (异步物理删除)
- [x] **组件对接 (Components)**
    - [x] `ServiceCard.tsx`: 状态标签支持 `Active / Deprecated` 切换
    - [x] `ServiceCard.tsx`: 实现负责人行内编辑 UI 与同步逻辑
    - [x] `ServiceCard.tsx`: 对接真正的流量统计 (`total_traffic_count` / `unique_traffic_count`)
    - [x] `ServiceCard.tsx`: 实现删除确认弹窗与接口调用
- [x] **页面整合 (Page)**
    - [x] `services/index.tsx`: 自动通过 Mutation 触发列表数据失效与刷新
- [x] **验证 (Verification)**
    - [x] 运行 `pytest` 确认端到端 CRUD 逻辑 100% 通过
    - [x] 运行脚本确认 "Unknown API" 脏数据已被前端接口成功过滤
