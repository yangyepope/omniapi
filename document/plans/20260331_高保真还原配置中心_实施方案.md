# 方案：高保真重构“全局配置中心” [2026-03-31]

## 1. 背景
用户反馈之前的实现与 Stitch 原型图完不匹配。当前原型展示了一个高度集成的单页面架构，包含一个内部二级侧边栏（配置菜单）和多个细分的功能视图（路由映射、流量处理、自动化策略、重放配置）。

## 2. 拟定变更

### UI 框架重构
#### [MODIFY] [index.tsx](file:///root/security-platform/frontend/src/routes/_layout/business-config/index.tsx)
- **容器化**：构建主页面骨架，包含顶层标题栏、配置菜单导航列、以及内容展示容器。
- **内部状态**：维护 `activeTab`，支持菜单项的平滑切换。
- **全局动作**：实现“导出 JSON”与“保存所有更改”按钮逻辑。

### 子视图组件化
#### [NEW] `src/components/BusinessConfig/`
- **RouteMappingView**: 还原路由前缀、微服务、责任人三栏表格。
- **TrafficControlView**: 还原滑块与标签云。
- **AutomationPolicyView**: 还原清理策略开关。
- **ReplayConfigView**: 还原复杂的并发与监控参数项。

### 视觉细节对齐
- **配色**：电光蓝 (#3B82F6) 主调。
- **圆角设计**：主体容器采用 `rounded-[3rem]`，菜单项采用 `rounded-2xl` 气泡风格。

## 3. 验证计划
- [ ] 菜单点击切换是否伴随 Framer Motion 动画。
- [ ] UI 比例、圆角、阴影是否与 Stitch 原型 1:1 对齐。
- [ ] 所有中文文案及副标题是否与图示一致。
