# 方案：同步“全局业务配置”设计及侧边栏集成 [2026-03-31]

## 1. 背景
用户要求从 Stitch 项目 `5537298166653667` 同步“全局业务配置”的 4 个页面设计，并在侧边栏新增“业务配置”入口。同步过程中需保持项目现有的 MD3 设计语言（`rounded-[2.5rem]`、蓝色强调色、精致阴影）。

## 2. 拟定变更

### 侧边栏集成 (Sidebar Integration)
#### [MODIFY] [Sidebar.tsx](file:///root/security-platform/frontend/src/components/layout/Sidebar.tsx)
- 在 `navItems` 中新增“业务配置”项。
- 选用 `LayoutGrid` 图标（Lucide）。

### 路由设计 (Routing Design)
#### [NEW] `src/routes/_layout/business-config/`
- 创建文件夹结构以支持 4 个子页面：
  - `index.tsx`: 列表/概览页。
  - `detail.tsx`: 详情页。
  - `editor.tsx`: 新增/编辑页。
  - `history.tsx`: 历史记录/版本页。

### 组件同步 (Stitch Synchronization)
基于 Stitch MCP 提供的灵感与布局，全量还原了以下高保真页面：
- **BusinessConfigList**: 4 列响应式网格布局，带标签搜索与状态过滤。
- **BusinessConfigDetail**: Bento Grid 风格的详情展示，含责任人信标与拓扑视图。
- **BusinessConfigEditor**: 沉浸式表单，集成大模型辅助校验提示。
- **BusinessConfigHistory**: 流式时间线布局，支持版本追溯与原子回滚按钮。

## 3. 验证结果
- [x] 导航同步：侧边栏菜单已生效，点击跳转正常。
- [x] 路由树：TanStack Router 已成功自动生成路由映射。
- [x] 风格一致性：全面采用 `rounded-[2.5rem]` 圆角、蓝色强调色及 MD3 阴影。
