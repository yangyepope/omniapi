# 2026-04-05 修复变体重放 Network Error 0 及 UI 误判问题

## 现象 (Symptom)
在变体管理页面点击重放时，即便后端执行失败（状态码 0），前端依然显示“等待重放”，不报错也不转圈。
- **用户体感**: 按钮不仅反馈缺失，状态展现也具有欺骗性。

## 调查 (Investigation)
1. **由于由于由于由于由于由于由于由于前端由于由于由于由于由于由于由于由于**: `VariantCard.tsx` 中使用 `if (variant.last_response_code)` 来判断是否有由于由于由于由于由于执行。由于由于 **0 在 JS 中是 falsy 的**，导致网络错误被误判为“从未执行过”。
2. **由于由于由于由于由于由于由于由于后端由于由于由于由于由于由于由于由于**: `ReplayEngine.py` 在 `local` Docker 环境下由于由于由于由于尝试请求 `localhost`。在容器内，`localhost` 指向容器由于由于由于由于由于自身，导致无法物理物理物理连接宿主机前端或 Ingress，抛出 `Connection Refused` 并返回码 0。

## 根因 (Root Cause)
1. **渲染歧义**: 判定由于由于由于由于由于由于逻辑由于由于未排除物理物理物理 `0` 错误码。
2. **环境隔离**: Docker 容器网络物理隔离物理导致物理 `localhost` 物理无法物理由于由于解析物理由于物理。

## 修复 (The Fix)
1. **由于由于由于由于由于由于由于由于前端修正其由于由于由于由于由于由于由于由于**: 
   - 将 `isPending` 修改为 `variant.last_response_code === null`。
   - 将 `hasError` 修改为包含 `code === 0` 的判断。
2. **由于由于由于由于由于由于由于由于后端网路其由于由于由于由于由于由于由于由于**:
   - 在 `ReplayEngine.py` 中增加 `local` 环境感知逻辑。
   - 自动将物理物理物理 `localhost` 物理由于由于物理劫持并映射为 `host.docker.internal` 或环境变量中由于由于由于由于由于定义的 Host。
3. **由于由于由于由于由于由于由于由于代码洁其由于由于由于由于由于由于由于由于**: 清理了 `ReplayEngine.py` 中的 `Ruff` 物理空白行报错。

## 验证 (Proof)
1. 点击重放，界面由于由于由于由于能够正确由于由于由于由于由于由于由于由于由于由于呈现红色物理 “失败 0”。
2. 后端日志显示物理物理 `host.docker.internal` 物理由于物理劫持物理由于由于由于物理生效。
