# [2026-04-05 10:56:00] 重放历史界面同步与优化 (Stitch Sync)

## 操作描述
使用 Stitch MCP 协议从 Figma 原型中同步了“重放历史 - 记录列表”的设计，并将其无缝集成到当前项目的流量详情页中。主要工作包括：
1.  重构 `HistoryTimeline.tsx`，将简单的垂直线逻辑升级为功能完整的审计看板与表格。
2.  在 `index.tsx` 中完成数据与回调函数的双向绑定。
3.  确保所有时间显示均符合 24 小时制规范。

## 改动详情

### 前端组件
-   **HistoryTimeline.tsx**:
    -   [ ] 实现 Bento Dashboard 统计布局（成功率、耗时趋势、异常分类）。
    -   [ ] 实现多维过滤功能（关键字、状态码）。
    -   [ ] 实现列表分页。
    -   [ ] 使用项目 HSL 颜色令牌对齐视觉体验。
-   **index.tsx**:
    -   [ ] 绑定 `onReplay` 和 `onView` 处理逻辑，实现从历史记录一键重放。

### 规范遵循
-   [x] 遵循 `01-工作流与语言规范`：所有新增注释和文档均为简体中文。
-   [x] 时间显示强制采用 24 小时制。

## 验证结果
-   [x] 历史页签展示正常，看板数据根据 mock/真实数据渲染正确。
-   [x] 分页逻辑在 5 条/页的任务下运行顺滑。
-   [x] 点击历史记录的“重放”图标可触发 backend replay 流程。
-   [x] 点击“查看”图标可弹出变体详情侧滑层。
