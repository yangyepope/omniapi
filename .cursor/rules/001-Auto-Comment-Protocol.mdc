---
name: "Auto-Comment Protocol"
version: "1.0"
globs:
  - "**/*"
alwaysApply: true
---

你是 OmniAPI 仓库的代码助手。除非用户明确要求“不要注释/保持无注释”，否则你必须默认执行以下【自动代码与配置注释协议】。

# 自动代码与配置注释协议 (Auto-Comment Protocol)

【类型】RULE（Cursor 规则文件）。这是规则总纲；按本文件 globs 生效范围自动附加到对话上下文中，不是 Trae Skill。
【优先级】P0（默认开启，最高优先级）：任何情况下都不能违反安全/隐私；其余细则在不冲突前提下执行。

## 触发场景（必须执行）
- 生成新的 FastAPI 路由、Schema、模型、服务层函数、依赖注入等后端代码。
- 修改 OmniAPI 的核心逻辑（鉴权、第三方接口调用、数据解析、异常处理、返回结构等）。
- 调整环境配置文件（`.env` / Settings）或 Docker 脚本（Dockerfile / compose / entrypoint 等）。
- 新增或调整第三方接口/URL（必须按“域名 + URI”拆分管理，并说明拼接规则）。

## 强制执行（代码段）
1) 每个新增/修改的函数都必须在函数定义上方写“中文注释块”，格式必须包含以下三行（可扩展，但三行必须存在）：

```python
# [设计意图]：解释为什么要引入这个逻辑/函数，解决什么问题，替代方案为什么不选
# [参数说明]：逐个说明输入参数的含义、格式、示例、边界条件
# [注意]：指出潜在的并发/性能/安全坑点，以及错误分支的处理策略
```

2) 如有复杂逻辑，必须在关键分支/关键数据结构附近补充行内中文注释，解释“为什么这样做”与“失败时如何处理”。

3) 返回 message 文案必须面向调用方清晰可操作；对外 message 不得包含内部异常堆栈/敏感信息。

## 强制执行（配置文件）
- 所有 `.env` 环境变量必须注明用途、格式、是否必须在生产覆盖、被哪些模块使用。
- Settings/第三方配置类中每个新增配置项必须说明：
  - 用途
  - 值格式（域名/URI/完整 URL/token 等）
  - 默认值风险（占位符；生产必须覆盖）
  - 依赖关系（哪些模块使用）
- URL/接口必须采用“域名 + URI（路径）”拆分管理，并说明拼接规则（例如 `rstrip('/')` 的原因）。

## 强制执行（Dockerfile）
- Dockerfile 中每一层构建指令（每个 `RUN/COPY/ENV/ARG/...`）必须写中文说明其目的与优化点（缓存命中、镜像体积、构建速度、安全性等）。

## 架构说明（新增文件必须）
- 新增文件必须在文件开头用多行中文注释说明：
  - 文件职责
  - 与现有项目结构的关系（Backend/Prestart/api/routes/services/core/config 等）
  - 被谁调用/如何被引入（入口、路由、依赖注入链路）

## 安全与隐私（强制）
- 不要在代码、注释、日志、示例中泄露任何密钥/Token/连接串；示例一律使用占位符。
- 如果需要日志：仅记录可排查的上下文，不记录敏感字段（token、password、secret 等）。
