---
name: "error_handler_standard (backend)"
version: "1.0"
globs:
  - "backend/**/*.py"
alwaysApply: true
---

# 后端开发模式 · 错误处理与返回标准（error_handler_standard）

【类型】RULE（Cursor 规则文件），用于后端开发模式；仅对 `backend/**` 范围生效（由本文件的 globs 决定），不是 Trae Skill。
【优先级】P1（后端模式细则）：在不违反 P0（安全/隐私/全局协议）的前提下执行。

本规则用于统一 backend 的异常处理与对外返回，保证调用方可预测、可排查，同时避免泄露敏感信息。

## 1) 错误分类（强制）

新增/修改后端逻辑时，必须按以下维度区分错误，并在代码注释/返回 message 中体现：

- 参数问题（客户端可修复）：400/422
  - message：明确指出哪个参数不合法、期望格式是什么
- 鉴权问题：401/403
  - message：不暴露“用户是否存在”等枚举信息
- 资源不存在：404
  - message：资源类型 + 标识（不含敏感信息）
- 上游服务异常（第三方/依赖）：502/503/504
  - message：提示“上游异常/超时/稍后重试”，必要时附上上游状态码（不要附 token/原始响应全文）
- 服务器内部错误：500
  - message：通用提示（系统繁忙/稍后重试），不回传堆栈

## 2) HTTPException 使用规范（强制）

- 在路由/依赖中优先使用 `HTTPException(status_code=..., detail=...)`。
- `detail` 面向调用方：可读、可操作；不要直接塞入 Python Exception 原始信息（尤其包含内部路径/堆栈）。
- 必要时给出 `headers={"WWW-Authenticate": "Bearer"}`（鉴权失败场景）。

## 3) 服务层异常处理（强制）

- 服务层（例如 `services/*`）捕获异常时：
  - 对外返回 message：稳定、可操作，不包含内部堆栈
  - 对内日志（如需要）：只记录排查必要信息，不记录 token/密码/连接串
- 如果需要“上游错误透传”，必须做字段过滤与长度限制，并写注释解释原因。

## 4) 返回结构一致性（强制）

- 同一个接口在成功/失败时，响应结构字段应保持一致：
  - `message`：始终存在（成功/失败都给）
  - `data`：成功为对象，失败为 `None`（或省略，但要一致）
- 不要在失败时突然改成完全不同的 JSON 结构（会破坏调用方解析）。

## 5) 注释要求（强制）

对每个新增/修改的函数，仍必须包含固定三行中文注释块：

```python
# [设计意图]：解释为什么要引入这个逻辑/函数，解决什么问题，替代方案为什么不选
# [参数说明]：逐个说明输入参数的含义、格式、示例、边界条件
# [注意]：指出潜在的并发/性能/安全坑点，以及错误分支的处理策略
```
