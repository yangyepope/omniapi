---
name: "comment_expert (backend)"
version: "1.0"
globs:
  - "backend/**/*.py"
  - "backend/**/Dockerfile"
  - "backend/**/*.yml"
  - "backend/**/*.yaml"
  - "backend/**/*.toml"
  - "backend/**/*.json"
alwaysApply: true
---

# 后端开发模式 · 注释专家（comment_expert）

【类型】RULE（Cursor 规则文件），用于后端开发模式；仅对 `backend/**` 范围生效（由本文件的 globs 决定），不是 Trae Skill。
【优先级】P1（后端模式细则）：在不违反 P0（安全/隐私/全局协议）的前提下执行。

本规则用于在“后端开发模式”下，把代码可读性与可维护性作为硬性约束：对 **backend/** 范围内的所有新增/修改内容，必须输出结构化中文注释与说明。

## 1) Python 函数/方法（强制）

对每个新增/修改的函数/方法，必须在函数定义上方写中文注释块，且必须包含以下三行（可扩展，但三行必须存在）：

```python
# [设计意图]：解释为什么要引入这个逻辑/函数，解决什么问题，替代方案为什么不选
# [参数说明]：逐个说明输入参数的含义、格式、示例、边界条件
# [注意]：指出潜在的并发/性能/安全坑点，以及错误分支的处理策略
```

并且：
- 如果函数是 FastAPI 路由/依赖：说明鉴权方式、输入来源（body/query/path/header）、返回结构。
- 如果函数涉及第三方请求：说明上游域名/URI/参数/鉴权/超时策略/失败分支。
- 如果函数涉及数据解析：说明关键字段来源、容错策略、缺失字段如何处理。

## 2) 复杂逻辑（强制）

当出现以下任意一种情况，必须添加行内注释解释“为什么这样做”：
- 多层 if/else、try/except 嵌套
- 数据结构嵌套访问（dict/list 深层路径）
- 重试、超时、降级、fallback
- 任何可能造成性能问题的逻辑（循环内 I/O、N+1、全量加载等）

## 3) 新增文件（强制）

新增任何文件时，必须在文件顶部写多行中文说明（注释/模块 docstring 均可），至少包含：
- 文件职责（做什么）
- 放在当前目录的原因（与 Backend/api/routes/services/core/config 的关系）
- 被谁调用/如何接入（入口、路由、依赖注入链路）

## 4) 配置与脚本（强制）

对 backend 内的配置/脚本文件：
- 每个新增配置项必须说明：用途、格式、默认值风险、依赖模块。
- URL/接口必须使用“域名 + URI（路径）”拆分管理，并写清拼接规则（例如 `rstrip('/')` 的原因）。
- Dockerfile 每一层指令都要用中文解释其目的与优化点（缓存、体积、安全、构建速度等）。

## 5) 安全（强制）

- 注释/示例/日志中不得出现真实密钥、Token、连接串；示例一律使用占位符。
- 对外 message 保持可读可操作，不暴露内部异常堆栈。
