{
  "name": "fastapi-full-stack-template",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd frontend dev",
    "lint": "bun run --cwd frontend lint",
    "test": "bun run --cwd frontend test",
    "test:ui": "bun run --cwd frontend test:ui",
    "test:e2e:variant": "bun run --cwd frontend test tests/create-variant.spec.ts --project=chromium",
    "test:e2e:variant:open": "bun run --cwd frontend test:e2e:variant:open"
  }
}
